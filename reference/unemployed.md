# Data about unemployed persons

Data about unemployed persons

## Usage

``` r
unemployed_total

unemployed_foreigners
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
814030 rows and 5 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
814030 rows and 5 columns.
