# Occupational group and region codes

Occupational group and region codes

## Usage

``` r
occupational_group_codes

region_codes
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 145
rows and 2 columns.

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 401
rows and 2 columns.
